[build]
builder = "DOCKERFILE"
dockerfilePath = "agents/examples/voice-assistant-sip-plivo/Dockerfile.cloudflare"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
